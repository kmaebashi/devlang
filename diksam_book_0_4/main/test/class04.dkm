public class Point {
    private double x;
    private double y;
    void print() {
	println("x.." + this.x + ", y.." + this.y);
    }
    constructor initialize(double x, double y) {
	this.x = x;
	this.y = y;
    }
}

// Pointクラスのインスタンスを生成
Point p = new Point(10, 20);
// その内容を表示する。
p.print();
